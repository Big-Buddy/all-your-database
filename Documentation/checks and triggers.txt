Ads:
1. Trigger for expired ads
2. Personals shouldn't be under the "selling" category
3. Trigger to decrement "Days to Promote" every day if the value isn't 0

Rented Spaces:
1. Trigger to prevent users from renting a space for a given StoreID for hours which exceed a maximum of 8 hours

Ratings:
1. Ensure a rating can't be over 5

Overall:
1. Trigger to send payment info to some kind of backup


set global event_scheduler = ON;

create event DeleteOldAds
on schedule every 1 day
do delete from Ads
where AdID in (
select AdID
from (select * from Ads join Users on Ads.PostingUserID = Users.UserID) as AdsAndUsers
where DaysToPromote <= 0 and datediff(now(), adddate(PostingDate,
  if(MembershipPlan = 'Premuim', 30, if(MembershipPlan = 'Silver', 14, 7)))) >= 0);